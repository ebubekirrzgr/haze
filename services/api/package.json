{
  "name": "@haze/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-transform-types --env-file-if-exists=.env src/server.ts",
    "start": "node --experimental-transform-types --env-file-if-exists=.env src/server.ts",
    "test": "node --test --experimental-transform-types src/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@haze/stellar": "workspace:*",
    "@hono/node-server": "^1.13.0",
    "@stellar/stellar-sdk": "^17.1.0",
    "@blend-capital/blend-sdk": "^3.3.1",
    "hono": "^4.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
